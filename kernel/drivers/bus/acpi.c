#include "acpi.h"#include "arch/i686/arch.h"#include "lib/log.h"#include "lib/string.h"#include "include/cast.h"#include "mm/vmm.h"#include "ui/console.h"static struct acpi_rsdp* rsdp = NULL;static struct acpi_rsdt* rsdt = NULL;static bool acpi_available = false;static uint32_t cpu_count = 0;static uint32_t local_apic_addr = 0;static uint32_t io_apic_addr = 0;static const struct acpi_fadt* cached_fadt = NULL;static bool acpi_checksum(void* data, const uint32_t length){    uint8_t sum = 0;    const uint8_t* ptr = (uint8_t*)data;    for (uint32_t i = 0; i < length; i++)    {        sum += ptr[i];    }    return sum == 0;}static struct acpi_rsdp* acpi_find_rsdp(void){    uint8_t* search_start = (uint8_t*)0x000E0000;    const uint8_t* search_end = (uint8_t*)0x00100000;    for (uint8_t* ptr = search_start; ptr < search_end; ptr += ACPI_RSDP_ALIGN)    {        if (memcmp(ptr, ACPI_RSDP_SIGNATURE, 8) == 0)        {            struct acpi_rsdp* candidate = (struct acpi_rsdp*)ptr;            if (acpi_checksum(candidate, 20))            {                return candidate;            }        }    }    return NULL;}static void acpi_parse_madt(struct acpi_madt* madt){    if (!madt)    {        return;    }    local_apic_addr = madt->local_apic_address;    cpu_count = 0;    uint8_t* ptr = madt->entries;    const uint8_t* end = (uint8_t*)madt + madt->header.length;    while (ptr < end)    {        struct acpi_madt_entry* entry = (struct acpi_madt_entry*)ptr;        switch (entry->type)        {            case ACPI_MADT_TYPE_LOCAL_APIC:            {                const struct acpi_madt_local_apic* lapic = (struct acpi_madt_local_apic*)entry;                if (lapic->flags & 1)                {                    cpu_count++;                    log_info_fmt("Found CPU - Processor ID: %d, APIC ID: %d",                                 lapic->processor_id, lapic->apic_id);                }                break;            }            case ACPI_MADT_TYPE_IO_APIC:            {                const struct acpi_madt_io_apic* ioapic = (struct acpi_madt_io_apic*)entry;                io_apic_addr = ioapic->io_apic_address;                log_info_fmt("Found I/O APIC - ID: %d, Address: 0x%x",                             ioapic->io_apic_id, ioapic->io_apic_address);                break;            }            case ACPI_MADT_TYPE_INT_OVERRIDE:            {                log_info("Found Interrupt Override entry");                break;            }        }        if (entry->length == 0)        {            break;        }        ptr += entry->length;    }    log_info_fmt("CPU Count: %d, Local APIC: 0x%x, IO APIC: 0x%x",                 cpu_count, local_apic_addr, io_apic_addr);}void acpi_init(void){    log_info("Initializing ACPI subsystem");    rsdp = acpi_find_rsdp();    if (!rsdp)    {        log_warn("RSDP not found, ACPI not available");        acpi_available = false;        return;    }    log_info_fmt("RSDP found at address 0x%x", PTR_TO_U32(rsdp));    const uint32_t rsdt_phys = rsdp->rsdt_address;    if (rsdt_phys == 0)    {        log_error("RSDT physical address is null, ACPI not available");        acpi_available = false;        return;    }    struct acpi_sdt_header* rsdt_header = vmm_map_temp(rsdt_phys, sizeof(struct acpi_sdt_header));    if (!rsdt_header)    {        log_error_fmt("RSDT not accessible at physical address 0x%x, ACPI not available", rsdt_phys);        acpi_available = false;        return;    }    if (rsdt_header->length < sizeof(struct acpi_sdt_header) || rsdt_header->length > 4096)    {        log_error_fmt("RSDT length invalid (%d bytes), ACPI not available", rsdt_header->length);        acpi_available = false;        return;    }    rsdt = (struct acpi_rsdt*)vmm_map_temp(rsdt_phys, rsdt_header->length);    if (!rsdt)    {        log_error_fmt("RSDT not accessible at physical address 0x%x, ACPI not available", rsdt_phys);        acpi_available = false;        return;    }    if (!acpi_checksum(rsdt, rsdt->header.length))    {        log_error("RSDT checksum invalid, ACPI not available");        acpi_available = false;        return;    }    log_info("RSDT checksum valid");    acpi_available = true;    log_info_fmt("Parsing RSDT with length %d", rsdt->header.length);    struct acpi_madt* madt = (struct acpi_madt*)acpi_find_table(ACPI_SIG_MADT);    if (madt)    {        log_info("MADT found, parsing entries");        acpi_parse_madt(madt);    }    else    {        log_warn("MADT not found");    }    cached_fadt = (const struct acpi_fadt*)acpi_find_table(ACPI_SIG_FADT);    if (cached_fadt)    {        log_info_fmt("FADT found, length %d, DSDT Address: 0x%x",                        cached_fadt->header.length, cached_fadt->dsdt);    }    else    {        log_warn("FADT not found");    }}bool acpi_is_available(void){    return acpi_available;}struct acpi_sdt_header* acpi_find_table(uint32_t signature){    if (!rsdt)    {        return NULL;    }    const uint32_t entry_count = (rsdt->header.length - sizeof(struct acpi_sdt_header)) / 4;    for (uint32_t i = 0; i < entry_count; i++)    {        const uint32_t phys = rsdt->tables[i];        struct acpi_sdt_header* peek = vmm_map_temp(phys, sizeof(struct acpi_sdt_header));        if (!peek)        {            log_error_fmt("Table at phys 0x%x not accessible", phys);            continue;        }        if (peek->signature != signature)        {            continue;        }        struct acpi_sdt_header* header = vmm_map_temp(phys, peek->length);        if (!header)        {            log_error_fmt("Table with signature %.4s not accessible at full length", (char*)&signature);            return NULL;        }        if (acpi_checksum(header, header->length))        {            return header;        }        log_error_fmt("Table with signature %.4s has invalid checksum", (char*)&signature);        return NULL;    }    return NULL;}uint32_t acpi_get_cpu_count(void){    return cpu_count;}uint32_t acpi_get_local_apic_address(void){    return local_apic_addr;}uint32_t acpi_get_io_apic_address(void){    return io_apic_addr;}void acpi_list_tables(void){    if (!rsdt)    {        log_info("ACPI not available\n");        return;    }    log_info("\nACPI Tables:\n");    log_info("============\n");    const uint32_t entry_count = (rsdt->header.length - sizeof(struct acpi_sdt_header)) / 4;    for (uint32_t i = 0; i < entry_count; i++)    {        //struct acpi_sdt_header* header = PTR_FROM_U32_TYPED(struct acpi_sdt_header, rsdt->tables[i]);        struct acpi_sdt_header* header = vmm_map_temp(rsdt->tables[i], sizeof(struct acpi_sdt_header));        if (!header)        {            log_error_fmt("Table %u at phys 0x%x not accessible", i, rsdt->tables[i]);            continue;        }        char sig[5];        memcpy(sig, &header->signature, 4);        sig[4] = '\0';        if (!acpi_checksum(header, header->length))        {            log_error_fmt("Table %.4s has invalid checksum", sig);        }        else        {            log_info_fmt("Table %.4s checksum valid", sig);        }        log_info_fmt("Table %.4s Length: %d", sig, header->length);        log_info_fmt("OEM ID: %.6s, OEM Table ID: %.8s", header->oem_id, header->oem_table_id);        log_info_fmt("Creator ID: 0x%x, Creator Revision: 0x%x",                     header->creator_id, header->creator_revision);    }    log_info_fmt("Total ACPI Tables: %d\n", entry_count);}const struct acpi_fadt* acpi_get_fadt(void){    return cached_fadt;}bool acpi_reset(void){    if (!acpi_available || !cached_fadt)    {        return false;    }    if (cached_fadt->header.length < sizeof(struct acpi_fadt))    {        log_warn("FADT length too small for reset register, cannot perform ACPI reset");        return false;    }    if (cached_fadt->reset_reg.address_space_id != ACPI_ADDR_SPACE_SYSTEM_IO || cached_fadt->reset_reg.address == 0)    {        log_warn("FADT reset register not usable (not system IO or null");        return false;    }    log_info_fmt("ACPI reset: port 0x%x, value 0x%x", (uint32_t)cached_fadt->reset_reg.address, cached_fadt->reset_value);    outb((uint16_t)cached_fadt->reset_reg.address, cached_fadt->reset_value);    return true;}static const uint8_t* acpi_find_s5(const struct acpi_sdt_header* dsdt){    const uint8_t* start = (const uint8_t*)dsdt + sizeof(struct acpi_sdt_header);    const uint8_t* end = (const uint8_t*)dsdt + dsdt->length;    if (end < start + 4)    {        return NULL;    }    for (const uint8_t* p = start; p <= end - 4; p++)    {        if (p[0] == '_' && p[1] == 'S' && p[2] == '5' && p[3] == '_')        {            return p;        }    }    return NULL;}static bool acpi_parse_s5(const uint8_t* s5, uint16_t* out_typa, uint16_t* out_typb){    const uint8_t* p = s5 + 4; // skip _s5_    if (*p != 0x12)    {        return false;    }    p++;    const uint8_t lead = *p;    const uint32_t extra_len_bytes = (lead >> 6) & 0x3;    p += 1 + extra_len_bytes;    p += 1;    if (*p == 0x0A)    {        p++;    }    *out_typa = *p;    p++;    if (*p == 0x0A)    {        p++;    }    *out_typb = *p;    return true;}bool acpi_shutdown(void){    if (!acpi_available || !cached_fadt || cached_fadt->pm1a_control_block == 0)    {        log_warn("ACPI shutdown not available (FADT missing or PM1a control block is 0)");        console_write("ACPI shutdown not available, cannot perform ACPI shutdown\n");        return false;    }    struct acpi_sdt_header* dsdt_header = vmm_map_temp(cached_fadt->dsdt, sizeof(struct acpi_sdt_header));    if (!dsdt_header || dsdt_header->length < sizeof(struct acpi_sdt_header))    {        log_warn("DSDT not accessible");        console_write("DSDT not accessible, cannot perform ACPI shutdown\n");        return false;    }    const struct acpi_sdt_header* dsdt = vmm_map_temp(cached_fadt->dsdt, dsdt_header->length);    if (!dsdt)    {        log_warn("DSDT not accessible at full length");        console_write("DSDT not accessible at full length, cannot perform ACPI shutdown\n");        return false;    }    const uint8_t* s5 = acpi_find_s5(dsdt);    if (!s5)    {        log_warn("_S5 package not found in DSDT");        console_write("_S5 package not found in DSDT, cannot perform ACPI shutdown\n");        return false;    }    uint16_t slp_typa = 0;    uint16_t slp_typb = 0;    if (!acpi_parse_s5(s5, &slp_typa, &slp_typb))    {        log_warn("Failed to parse _S5 package");        console_write("Failed to parse _S5 package, cannot perform ACPI shutdown\n");        return false;    }    log_info_fmt("ACPI S5: SLP_TYPa=0x%x SLP_TYPb=0x%x PM1a=0x%x PM1b=0x%x", slp_typa, slp_typb, cached_fadt->pm1a_control_block, cached_fadt->pm1b_control_block);    console_write("ACPI shutdown: SLP_TYPa=0x");    console_write_hex(slp_typa);    console_write(" SLP_TYPb=0x");    console_write_hex(slp_typb);    console_write(" PM1a=0x");    console_write_hex((uint16_t)cached_fadt->pm1a_control_block);    if (cached_fadt->pm1b_control_block)    {        console_write(" PM1b=0x");        console_write_hex((uint16_t)cached_fadt->pm1b_control_block);    }    console_write("\n");    const uint16_t SLP_EN = 1U << 13;    outw((uint16_t)cached_fadt->pm1a_control_block, slp_typa | SLP_EN);    if (cached_fadt->pm1b_control_block)    {        outw((uint16_t)cached_fadt->pm1b_control_block, slp_typb | SLP_EN);        console_write("ACPI shutdown command sent to PM1a and PM1b control blocks\n");    }    return true;}